# Context Notes — ShotStage 고도화 버전

이 문서는 작업 중 내려진 결정과 그 이유를 기록한다. 다음 세션(사람이든 에이전트든)이 왜 이렇게 설계했는지 다시 유추하지 않아도 되도록 계속 이어서 기록한다.

## 원본 사이트 분석 관련
- 원본(https://shotstage.vercel.app/)은 클라이언트 사이드 렌더링 SPA라 web_fetch로는 페이지 타이틀 외 콘텐츠를 가져올 수 없었다. 사용자가 제공한 스크린샷 3장(메인 화면, 프롬프트 패널, 결과 모달)을 근거로 기능을 역추적했다.
- 원본 소스코드는 확보하지 못했으므로, 기존 기능도 처음부터 재구현해야 한다 (checklist.md Phase 1~3).
- 결과 모달의 프롬프트 문구에 "Use the attached blocking frame only as the camera reference... Ignore its plain placeholder surfaces and colours"라는 문장이 있다. 즉 3D 뷰포트는 의도적으로 무채색 placeholder로 남겨두고, 실제 디테일은 AI 생성 단계에서 채우는 설계다. 이 설계 철학이 이후 DOF 기능을 제외하는 근거가 되었다.

## 범위 결정
- 타겟 사용자: 개인 크리에이터(1인 작업). 팀 협업/계정/서버 저장은 이번 범위에서 제외.
- 우선순위로 선택된 두 영역: (1) 카메라 고급 기능, (2) 캐릭터·환경 에셋 라이브러리.
- 제외된 영역(향후 별도 PRD 대상): 멀티 샷/스토리보드 관리, AI 모델·생성 연동 확장(여러 모델·프롬프트 히스토리·직접 생성), 팀 협업.

## 자체 점검을 통해 축소한 항목 (중요 — 재검토 시 참고)
최초 초안에는 아래 세 가지가 더 있었으나, "원본은 단일 화면·한 번에 하나 확정하는 흐름"이라는 판단 기준으로 자체 점검 후 제외했다.

1. **심도(DOF) 실시간 블러 렌더링** — 제외 이유: 3D 뷰포트가 애초에 "무시하라"고 명시하는 placeholder라, 렌더링 비용 대비 실익이 없음. React Three Fiber post-processing 파이프라인 추가 비용도 상당함.
2. **무빙샷 시작/끝 2단계 프레임 캡처** — 제외 이유: 프레임을 2장 관리하는 완전히 새로운 워크플로우라 원본의 "한 번에 하나 확정" 철학과 충돌. 대신 프레임 1장 + 무브 프리셋(방향/강도/지속시간)만 추가하는 방식으로 축소.
3. **에셋 라이브러리 별도 관리 화면** — 제외 이유: 원본에 없던 "페이지" 개념을 추가하는 것이라 과함. 대신 입력창 옆 드롭다운 안에서 inline 삭제(hover 시 × 아이콘)와 덮어쓰기 저장으로 대체.

**주의**: 이 세 가지를 나중에 다시 추가하고 싶다는 요청이 있으면, 이 판단 기준(원본의 단일 화면·즉시 확정 흐름 유지)에 부합하는 대안적 구현 방식을 먼저 고민한 뒤 추가할 것. 무조건 원안(2단계 캡처 등)으로 되돌리지 말 것.

## 사용성 재검토 후 보완한 항목
- 무빙샷 강도(약함/보통/강함)를 미리보기 없이 감으로 선택해야 한다는 문제가 사용 시나리오 점검 중 발견됨.
- 해결책으로 "미리보기 프레임 재도입"(2단계 캡처 부활)과 "설명 툴팁 추가" 두 가지를 검토했고, 사용자가 가벼운 쪽(툴팁)을 선택함.
- 각 강도 버튼에 짧은 설명 문구를 툴팁으로 추가하기로 결정 (PRD 5.3절, checklist Phase 5).

## 기술 스택 결정 및 근거
- 저장소: IndexedDB(브라우저 로컬 저장)만 사용. 서버/DB/인증 없음. 근거: 개인 크리에이터 타겟 + MVP 범위 + 협업 비목표.
  - 주의: 추후 여러 기기 동기화나 팀 협업이 필요해지면, 이 저장 방식부터 재설계가 필요함.
- 프레임워크: Next.js(App Router) + React Three Fiber/drei + Zustand + Tailwind CSS. 원본도 유사한 스택으로 추정되나 확인된 사실은 아님(가정).
- 배포: GitHub 저장소 → Vercel (사용자가 처음부터 명시한 요구사항).

## 구현(Phase 0~6) 진행 중 내려진 세부 결정
- **개발 환경 제약**: 이 작업 환경에는 헤드리스 브라우저가 없어, 실제 렌더링 화면을 스크린샷으로 찍어 원본과 픽셀 단위로 비교하는 시각적 검증을 하지 못했다. `npm run build`와 `npm run lint` 통과만 확인한 상태. 사용자가 로컬에서 `npm run dev`로 실행해 육안 확인을 하는 과정이 필요하다.
- **카메라 컨트롤 구조**: 3D 카메라의 실제 위치/각도(yaw, pitch, position)는 React 상태가 아니라 `CameraRig` 내부의 ref로 관리하고, three.js 카메라 객체를 `useFrame` 안에서 직접 mutate하는 R3F의 표준 패턴을 따랐다. HUD에 표시할 거리/뷰 방향 정보만 10프레임에 한 번씩 zustand 스토어로 던진다 (매 프레임 리렌더 방지).
  - 이로 인해 Next.js 16의 React Compiler 계열 ESLint 규칙(`react-hooks/immutability`)이 오탐(false positive)을 일으켜서, `src/components/viewport/**` 경로에 한해 해당 규칙을 껐다 (`eslint.config.mjs` 참고). R3F를 쓰는 이상 불가피한 예외이며, 다른 경로에는 규칙이 그대로 적용된다.
- **마네킹 정면 방향 규약**: 마네킹의 눈(얼굴)은 로컬 좌표계 기준 +Z 방향을 향하도록 배치했다. 샷 타입 프리셋의 카메라는 피사체의 +Z 쪽(정면)에서 바라보도록 스냅한다. Front/Back/Side 판정은 "피사체→카메라 벡터"와 "피사체 정면 벡터"의 내적으로 계산한다 (0.5 이상이면 front, -0.5 이하면 back, 그 사이는 side).
- **Bird's-eye 뷰 구현 방식**: 별도의 카메라 상태를 두지 않고, 기존 fly 카메라의 위치/시점(ref)은 그대로 둔 채 렌더링되는 프레임에서만 상공 시점으로 오버라이드한다. 그래서 Shot view로 되돌아가면 이전 시점이 그대로 복원된다.
- **프레임 캡처 방식**: `Canvas`의 `gl` 옵션에 `preserveDrawingBuffer: true`를 켜고, Generate 클릭 시 store의 `frameCaptureRequestId`를 증가시켜 Canvas 내부의 `FrameCapture` 컴포넌트가 `gl.domElement.toDataURL()`로 캡처하도록 구현했다.
- **AI 모델 드롭다운**: PRD에서 "AI 모델 연동 확장"은 명시적으로 이번 범위 밖이라고 했지만, 원본 스크린샷에도 이미 모델 선택 드롭다운(`GPT Image 2`)이 있었으므로 이건 신규 기능이 아니라 기존 UI 재현으로 판단해 그대로 넣었다. Image 모드에는 `GPT Image 2 / Nano Banana Pro / Flux`, Video 모드에는 `Higgsfield / Runway / Kling`을 정적 목록으로 넣었을 뿐, 실제 어떤 모델과도 API 연동은 하지 않는다 (프롬프트 텍스트만 생성).
- **IndexedDB 스키마**: PRD 6.4절 설계 그대로 `src/lib/db.ts`에 구현. 이름이 같은 프리셋을 저장하면 기존 id를 재사용해 덮어쓰는 방식으로 "이름 변경 없이 덮어쓰기 저장"을 구현했다 (PromptPanel의 `handleSaveCharacter` 등에서 `items.find(p => p.name === name)`으로 기존 항목을 찾음).
- **참조 이미지 용량**: 업로드된 이미지를 리사이즈/압축하지 않고 원본 그대로 data URL로 저장하고 있다. PRD 9절에 남겨둔 오픈 이슈 그대로 미해결 상태이며, 실사용 중 용량 문제가 확인되면 리사이즈 로직을 추가해야 한다.

## 배포 후 발견된 버그 (2026-07-15, 사용자 배포 테스트 중 발견)
- **증상**: Vercel 배포 후 실제로 열어보니 3D 뷰포트가 완전히 검게 나오고 룸/마네킹이 전혀 보이지 않음. 라벨("two shot · 43mm · eye level" 등)은 정상 표시되어, 상태 관리 자체는 문제없었음.
- **원인**: `CameraRig.tsx`에서 샷 프리셋 스냅과 Recenter 기능이 카메라가 바라볼 방향을 계산할 때, 임시 더미 오브젝트로 일반 `THREE.Object3D()`를 만들어 `.lookAt()`을 호출했다. three.js에서 `Object3D.lookAt()`과 `Camera.lookAt()`은 내부적으로 인자 순서가 반대라(카메라/라이트는 "target을 eye처럼, this.position을 target처럼" 취급) 회전 방향이 정반대로 계산된다. 그 결과 카메라 위치는 피사체 앞쪽으로 정확히 이동했지만, 실제로 바라보는 방향은 피사체 반대쪽(방의 앞쪽 열린 면 바깥, 배경색만 있는 허공)이었다. 초기 카메라 상태(`yawRef` 초기값)도 같은 방향 오류가 있어서, 첫 로딩 화면부터 검게 보였다.
- **검증 방법**: 이 환경에는 헤드리스 브라우저가 없어 실제 화면 캡처는 못 했지만, `three` 패키지를 노드 스크립트에서 직접 불러와 `Object3D` 더미와 `PerspectiveCamera` 더미로 각각 `lookAt()` 결과를 비교해 방향이 정반대로 나오는 것을 수치로 확인했다.
- **수정**: 더미 오브젝트를 `new THREE.Object3D()` 대신 `new THREE.PerspectiveCamera()`로 교체(샷 프리셋 스냅, Recenter 두 곳 모두), 초기 `yawRef` 값을 `Math.PI`에서 `0`으로 수정. Bird's-eye 뷰의 `camera.lookAt()`은 실제 카메라 인스턴스에 직접 호출한 것이라 원래부터 문제 없었음.
- **교훈**: three.js에서 임의의 오브젝트 방향을 "카메라가 어딘가를 보게 하고 싶을 때"의 참조용으로 계산할 때는 반드시 `Camera`(또는 `isCamera: true`인) 인스턴스를 더미로 써야 한다. 일반 `Object3D`로 대체하면 회전이 반대로 나온다.

## v1.1.0 — 사용자 피드백 5건 반영 (2026-07-15)
1. **이미지 모델에 미드저니 누락**: `IMAGE_MODELS` 목록에 `Midjourney` 추가.
2. **3D 모델이 못생김 → 사람 모양으로 변경**: 기존의 뭉툭한 로봇형 캡슐 조합 대신, 실제 사진작가들이 카메라 블로킹용으로 쓰는 "목재 관절 아트 마네킹"을 참고해 재설계했다. 머리-목-어깨관절-가슴-허리-골반-팔(위팔/팔꿈치/아래팔/손)-다리(허벅지/무릎/종아리/발) 구조로 사람 비율에 가깝게 나누고, 관절 부위에 구슬 모양을 넣어 아트 마네킹 특유의 실루엣을 표현했다. 색도 로봇 같은 무채색 그레이 대신 따뜻한 우드톤(`#cbb28f`)으로 바꿨다. 다만 이 환경엔 헤드리스 브라우저가 없어 실제 렌더링 결과를 직접 보고 다듬지는 못했다 — 로컬에서 확인 후 비율이 어색하면 알려달라고 요청할 것.
3. **Two Shot 선택 후 다시 한 명으로 못 돌아가는 버그**: `setShotType`에서 `showSecondSubject`를 `preset.showSecondSubjectByDefault ?? 기존값` 방식으로 처리하고 있었는데, Two Shot/Over Shoulder가 아닌 프리셋들은 `showSecondSubjectByDefault`가 `undefined`라 "기존값을 그대로 유지"하는 방향으로 동작했다. 즉 한 번 Two Shot을 눌러 두 번째 피사체가 켜지면, 이후 다른 샷 타입으로 바꿔도 계속 두 명이 나왔다. 모든 프리셋에 `showSecondSubjectByDefault: boolean`을 명시적으로 지정하고, `setShotType`이 항상 그 값으로 덮어쓰도록 수정했다 (Two Shot/Over Shoulder만 `true`, 나머지는 `false`). 물론 SUBJECT 패널의 체크박스로 언제든 수동으로도 켜고 끌 수 있다.
4. **메뉴 한국어화**: 애초에 "한국어로 만들겠다"고 했었는데 패널 UI(SHOT TYPE/CAMERA/SUBJECT/PROMPT 등)를 영문 그대로 만든 걸 사용자가 지적함. 헤더는 이미 한국어였지만 나머지를 놓쳤던 실수. 아래처럼 나눠서 처리했다.
   - **UI 라벨/버튼/메뉴**: 전부 한국어로 번역 (예: "Lens (FOV)" → "렌즈 (화각)", "Recenter on subject" → "피사체 중심으로 재정렬", 샷 타입 버튼도 "미디엄 샷/와이드 샷/클로즈업/..." 등).
   - **HUD 라벨(뷰포트 위 오버레이)**: `SHOT_PRESETS`에 `label`(한국어 표시용)과 `promptName`(영문, AI 프롬프트용)을 분리해뒀던 구조를 활용해, HUD는 `label` + 한국어 앵글 라벨(`ANGLE_LABEL_KO`)을 쓰도록 변경. 예) "투 샷 · 정면 · 아이레벨 · 43mm".
   - **최종 AI 프롬프트 텍스트(결과 모달에 뜨는 영문 문장)는 의도적으로 영문 그대로 유지**했다. GPT Image/Higgsfield 등 대상 AI 툴들이 영문 프롬프트에 더 최적화되어 있다고 보고, "메뉴는 한국어, 실제 생성 프롬프트는 영문"으로 역할을 나눈 것. Subject/Environment/Look-style 입력창의 placeholder 예시도 같은 이유로 영문 그대로 뒀다. 이 부분은 명시적으로 사용자에게 확인이 필요한 결정이라 대화창에서 알림.
   - 브랜드/모델 고유명사(GPT Image 2, Midjourney, Higgsfield, Runway, Kling, SHOTSTAGE 등)는 번역하지 않음.
5. **버드아이 모드에서 화면 이동 안 됨**: 기존엔 Bird's-eye일 때 카메라를 피사체 바로 위 고정 좌표(subjectPos, 높이 9)로 매 프레임 강제 고정해서, WASD를 눌러도 화면이 안 움직였다. `posRef`(WASD/Q,E로 계속 갱신되는 실제 카메라 위치)를 그대로 활용해 x/z는 평면 이동, y는 고도(줌 느낌)로 쓰도록 수정 — `camera.position.set(posRef.x, max(3, posRef.y+6), posRef.z)`. Shot view로 돌아가면 posRef가 그대로 남아있어 원래 시점이 복원되는 기존 설계는 유지됨.

## v1.2.0 — 사용자 피드백 7건 반영 (2026-07-16)
1. **클로즈업이 얼굴에 포커스 안 됨**: `closeUp` 프리셋의 `heightOffset`이 +0.5(눈높이보다 위)였고, 카메라가 바라보는 지점(focal)이 모든 샷 타입에 하드코딩된 torso 높이 1.2였다. `ShotPreset`에 `focalHeight` 필드를 추가해 프리셋마다 바라보는 높이를 다르게 지정할 수 있게 하고, `closeUp`은 `distance: 0.9, heightOffset: 0, focalHeight: 1.62`(눈 높이)로 조정. `CameraRig`의 pendingSnap/recenter 모두 `focalHeight`를 참조하도록 수정.
2. **오버숄더가 오버숄더가 아님 / 3. 축소하면 둘 다 같은 방향**: 두 가지가 사실 같은 원인. `SECOND_SUBJECT_OFFSET`으로 두 번째 피사체 위치를 첫 번째 피사체 대각선 앞쪽(x+0.9, z+0.3)에 두고, `Math.atan2`로 항상 첫 번째 피사체 쪽을 바라보도록 회전을 계산(Scene.tsx). 카메라 쪽은 `overShoulder` 프리셋일 때 전용 분기를 타서, "두 번째 피사체 위치에서 첫 번째 피사체 방향으로 조금 더 나아간 지점"(= 두 번째 피사체 어깨 바로 뒤)에 카메라를 두고 첫 번째 피사체의 얼굴(focalHeight)을 바라보게 했다 (`CameraRig.tsx`의 `pendingSnap.shotType === "overShoulder"` 분기). Node 스크립트로 두 인물 모두 프러스텀 안에 들어오는지, B의 회전이 실제로 A를 향하는지 수치로 검증함 (실제 렌더링 화면은 여전히 못 봄).
3. **화각비 눌러도 변화 없음**: 로직 자체(bar 계산)는 정상이었을 가능성이 높고, 실제 원인으로 가장 유력한 건 레터박스/필러박스 바 색상이 순수 검정(#000)인데 3D 씬 배경색도 거의 검정(#05070c)이라 대비가 거의 없어서 안 보였을 것이라는 점. 바 안쪽 경계에 accent 색(주황) 보더 라인을 추가하고, 크롭이 적용되면 우측 하단에 "{비율} 크롭 적용됨" 배지를 표시하도록 함. 기본 그리드(`showGrid`)도 `true`로 바꿔서 어두운 화면에 참조선이 항상 보이게 함.
4. **깊이 슬라이더가 안 움직이는 것처럼 느껴짐**: 슬라이더 자체 버그는 코드상 발견 못 함(좌우 슬라이더와 동일 패턴). 다만 깊이(Z축)는 카메라 시선 방향과 같은 축이라 좌우 이동보다 화면상 변화가 훨씬 약하게 느껴질 수 있음 — 좌우는 1.4m, 깊이는 2.4m 범위로 넓혀서 체감을 키움. **실제로 UI 슬라이더 핸들 자체가 반응이 없는 문제였다면 이 조치로는 해결이 안 될 수 있음 — 여전히 안 움직이면 알려달라고 요청 필요.**
5. **캐릭터/환경시트 설명 툴팁**: 체크박스 라벨 옆에 물음표(?) 아이콘을 추가하고 `title` 속성으로 "이 시트가 무엇을 위한 것이고, 업로드하면 프롬프트에 어떤 문구가 자동으로 추가되는지"를 설명. 네이티브 브라우저 툴팁이라 별도 라이브러리 없이 구현.
6. **참고 문서 3종 반영**: Midjourney v8.1 가이드(PDF), GPT Image 2 한국어 Knowledge 문서(md), 나노바나나 프로 AI 필름메이킹 템플릿(docx)을 각각 확인하고, `promptBuilder.ts`에 모델별 프롬프트 빌더를 분리했다.
   - **Midjourney**: 짧고 직접적인 문장, 주 피사체를 문장 맨 앞(문법적 주어)에 배치, "use/make/create" 같은 명령형 대신 서술형, 파라미터(`--ar`, `--v 8.1`)는 맨 끝에만. 화각비는 정수비만 지원해서 `2.35:1`은 `21:9`로 근사 매핑(`MIDJOURNEY_ASPECT_MAP`).
   - **GPT Image 2**: `[스타일/매체] → [피사체] → [배경] → [조명/분위기] → [제외 요소]` 슬롯 구조를 따르고, 문서에서 권장하는 `no extra text, no logo, no watermark` 같은 부정 프롬프트를 항상 마지막에 추가.
   - **Nano Banana Pro**: `Composition:`/`Lighting:` 라벨 구조와 "eye-line match", "rule-of-thirds" 같은 촬영 전문 용어를 그대로 채용. 오버숄더/투샷일 때는 문서의 3번(Dialogue Two-Shot/OTS) 템플릿을 따라 "Over-the-shoulder two-shot of ... framed from the second person's shoulder" 식으로 구성. 캐릭터/환경시트가 있으면 문서의 11번(Consistency Core) 문구(`[Consistency Core]: Do not allow identity drift; force-match...`)를 그대로 채용.
   - Higgsfield/Runway/Kling(비디오 모델)과 Flux는 참고 문서가 없어 기존 범용 템플릿(`buildGenericPrompt`)을 그대로 사용.
- **검증 한계**: 이번에도 헤드리스 브라우저가 없어 실제 3D 렌더링 결과나 UI 인터랙션은 눈으로 확인하지 못했다. 카메라 위치/방향 계산은 매번 Node에서 `three` 패키지를 직접 불러와 프러스텀 포함 여부, 방향 벡터 일치 여부 등을 수치로 검증하는 방식으로 대체하고 있다. 실제 화면에서 어색한 부분이 있으면 스크린샷으로 계속 알려주는 게 정확한 디버깅에 필수적이다.

## v1.3.0 — 오버숄더/투샷/버드아이 추가 수정 (2026-07-16)
사용자가 스크린샷과 함께 "오버숄더가 인물 얼굴 덩어리로 꽉 찬다", "투샷에서 두 인물 위치를 각자 조정할 수 없다", "버드아이에서 드래그가 안 된다"고 재차 피드백.

1. **오버숄더 카메라가 인물 속에 파묻히는 문제**: 원인은 카메라를 "두 번째 피사체 위치에서 0.4m만 더 간 지점"에 뒀는데, 마네킹 머리/어깨 반경이 그것과 비슷해서였다. `overShoulder` 프리셋의 `distance`를 0.75로 늘리고, A-B축과 완전히 일치하지 않도록 수직 방향으로 0.2m 옆으로 비켜서게(`OTS_LATERAL_NUDGE`) 만들었다. Node에서 three.js로 재검증: 카메라-A 거리 2.03m, 카메라-B(어깨) 거리 0.78m, 둘 다 프러스텀 안에 들어옴.
2. **두 번째 피사체 위치를 독립적으로 조정 불가능**: 근본 원인은 애초에 "두 번째 피사체 전용 상태/슬라이더 자체가 없었다"는 것 — B의 위치를 항상 A 기준 상대 오프셋으로 자동 계산만 하고 있었다. `secondSubject: SecondSubjectState`(좌우/깊이/회전)를 스토어에 별도로 추가하고, `SubjectPanel`에 "인물 2" 섹션(두 번째 피사체 표시 체크 시에만 노출)을 추가해 완전히 독립적으로 조정 가능하게 했다. 샷 타입을 바꿀 때마다 `SECOND_SUBJECT_DEFAULT`(좌우 0.3, 깊이 0.5, 회전 -161°)로 리셋되고, 그 이후 자유롭게 재조정 가능.
3. **버드아이에서 드래그 안 됨**: 원인은 드래그 핸들러가 항상 `yaw/pitch`만 바꾸는데, 버드아이 렌더링은 `yaw/pitch`를 아예 안 쓰고 항상 수직으로 내려다보기만 해서 드래그가 시각적으로 아무 효과가 없었던 것. `viewModeRef`를 추가해 최신 `viewMode`를 드래그/스크롤 핸들러 클로저 안에서 읽을 수 있게 하고, 버드아이일 때는 드래그 = 평면 이동(패닝), 스크롤 = 고도 조절(줌)로 분기 처리했다.
- **검증 한계**: 이번에도 실제 렌더링 화면은 못 보고 Node에서 three.js 프러스텀 계산으로만 검증했다. 사용자가 준 스크린샷 덕분에 "카메라가 인물 속에 파묻혀 있다"는 원인을 비교적 정확히 짚을 수 있었음 — 앞으로도 어색한 장면은 스크린샷으로 받는 게 가장 빠른 디버깅 경로.

## v1.4.0 — 카메라 조작 방식 전면 개편 + 프레임 캡처 크롭 반영 (2026-07-16)
1. **"드래그 회전 반경이 너무 넓다" / "인물 중심으로 회전하고 싶다"**: 기존 방식은 "카메라가 제자리에서 고개만 돌리는" free-look이라, 조금만 드래그해도 피사체가 화면 밖으로 쉽게 벗어났다. 이를 **피사체를 중심(피벗)에 두고 카메라가 구체 표면을 도는 궤도 회전(orbit)**으로 전면 교체했다.
   - 상태를 `pivotOffset`(피사체 기준 추가 오프셋) + `orbitYaw/orbitPitch`(각도) + `orbitRadius`(거리)로 재구성. 매 프레임 `camera.position = pivot - forward(yaw,pitch)*radius`로 계산하고 항상 `pivot`을 바라보게 해서, 회전해도 피사체가 항상 화면 중앙에 고정된다.
   - Node에서 검증: 궤도 각도를 90° 돌린 후에도 피사체의 화면상 NDC 좌표가 정확히 (0,0)(정중앙)으로 나옴을 확인.
   - 샷 타입 프리셋 스냅 시 기존 위치→(yaw,pitch,radius)로 정확히 역산되는지도 재구성 오차 0으로 확인.
   - 드래그 민감도도 0.0025→0.0018로 살짝 낮춤.
2. **스페이스바 = 핸드 툴(팬)**: 스페이스바를 누른 채 드래그하면 궤도 회전 대신 피벗을 화면 기준 좌우/상하로 이동(팬)하도록 추가. WASD/Q,E는 그대로 피벗을 이동하는 용도로 유지(의미만 "카메라 위치"에서 "궤도 중심점 위치"로 바뀜).
   - **중요 버그 발견 및 수정**: 스페이스바 키 감지를 `window`에 전역으로 걸었더니, Subject/Environment/Style 같은 텍스트 입력창에 스페이스(공백)를 타이핑할 때도 가로채는 문제가 생길 뻔했다. `isTypingTarget()` 가드를 추가해 INPUT/TEXTAREA/SELECT/contentEditable 요소에 포커스가 있을 때는 단축키 처리를 하지 않도록 방지. **이 가드는 기존 WASD 키도 함께 보호해서, 사실 이전 버전에 있던 "입력창에 w/a/s/d 타이핑하면 카메라도 같이 움직이는" 잠재 버그까지 같이 고쳐진 것**(사용자가 보고하진 않았지만 코드 리뷰 중 발견).
3. **버드아이에서 샷 선택 시 인물이 중심에 안 옴**: 원인은 버드아이의 중심 좌표가 "샷 뷰 카메라가 있었을 위치"(`posRef`, 피사체로부터 `distance`만큼 떨어진 지점)를 그대로 재사용하고 있었기 때문. 버드아이 전용 상태(`birdPanRef`, `birdHeightRef`)를 완전히 분리하고, 샷 타입을 바꿀 때마다 `birdPanRef`를 (0,0)으로 리셋해서 항상 피사체가 정중앙에서 시작하도록 수정.
4. **화각비가 "미묘하게 틀리다"(특히 1:1)**: `AspectRatioMask`의 레터박스 계산 자체는 맞았지만, **`FrameCapture`가 이 크롭을 전혀 반영하지 않고 캔버스 전체(크롭 전 원본)를 그대로 캡처**하고 있었다. 컨테이너가 넓을수록(기본 상태) 크롭 폭이 큰 1:1에서 이 불일치가 가장 두드러지게 나타난 것으로 추정. `FrameCapture.tsx`에 `cropToAspectRatio()`를 추가해 `AspectRatioMask`와 동일한 계산식으로 오프스크린 캔버스에 중앙 크롭 후 캡처하도록 수정. Node로 1600×900 기준 1:1→900×900(정확히 1.0), 9:16→506×900(0.5625), 16:9→그대로(1.7778) 나오는 것을 확인.
- **검증 한계**: 이번에도 실제 화면·마우스 조작감은 못 보고 Node의 three.js 수치 계산으로만 검증했다. 특히 "회전 반경이 좁아진 느낌"이나 "핸드 툴 사용감"은 실제 체감의 영역이라 사용자 테스트가 꼭 필요하다.

## v1.5.0 — 인물별 색상 구분 + 헤더/오버레이 시각 정리 (2026-07-16)
1. **"왼쪽 상단 키보드 컨트롤 안내가 가려짐"**: 정확한 원인은 스크린샷만으로 100% 확신하지 못했다. 가장 유력한 후보 두 가지에 모두 대응했다.
   - 직전 버전(v1.4.0)에서 헤더 상단 우측의 단축키 힌트를 "drag 궤도 회전 · space+drag 화면 이동 · scroll 줌 · WASD/Q,E 중심점 이동"으로 늘렸는데, `kbd` 태그가 여러 개 붙으면서 이전보다 훨씬 길어졌다. `justify-between` 레이아웃에서 좁은 화면이면 로고 영역과 겹치거나 잘릴 수 있어 대폭 축약하고(`drag 회전 · space 이동 · scroll 줌 · WASD 중심점`), 노출 breakpoint도 `md`→`lg`로 올려 좁은 화면에서는 아예 숨기도록 함.
   - 뷰포트 좌측 상단의 장식용 코너 브라켓과, 화각비 크롭 시 상단에 그어지던 accent 색 보더 라인(레터박스 바 경계선)이 같은 영역에서 시각적으로 겹칠 가능성이 있어, 보더 라인을 제거하고 크롭 안내는 우측 하단의 "OO 크롭 적용됨" 배지로만 하도록 단순화.
   - **확실하지 않은 진단**이라 사용자에게 여전히 문제가 있으면 어느 요소가 정확히 가려지는지 다시 스크린샷으로 확인이 필요하다고 안내함.
2. **인물 1/인물 2 색상 구분**: `lib/subjectColors.ts`에 `SUBJECT_COLORS.primary`(기존 우드톤)/`secondary`(블루 톤)를 정의하고, `Mannequin` 컴포넌트가 `bodyColor`/`jointColor`를 prop으로 받도록 수정. `Scene.tsx`에서 첫 번째/두 번째 마네킹에 각각 다른 색을 전달. `SubjectPanel`의 슬라이더는 Tailwind의 고정된 `accent-orange-500` 대신 인라인 `style={{ accentColor }}`로 값을 받아, 3D 마네킹 색과 패널 슬라이더 색이 정확히 같은 hex 값을 쓰도록 했다(네이티브 `<input type=range>`의 `accent-color` CSS 속성 활용). "인물 1"/"인물 2" 라벨 앞에도 같은 색의 작은 원형 스와치를 추가.

## v1.6.0 — 조작 힌트 복원 + 재정렬 버튼 수정 + 관절 계층 구조 마네킹 (2026-07-16)
1. **뷰포트 좌상단 조작 힌트가 실제로 빠져 있었음**: 확인해보니 사용자 말이 맞았다. `ViewportOverlay.tsx`에는 애초에 그 자리에 키보드/마우스 힌트 텍스트가 없었다(v1.5.0에서 헤더 쪽 힌트만 손봤을 뿐, 뷰포트 좌상단에는 원래 라벨만 있었음). 상단 왼쪽 라벨 옆에 "drag 회전 · space 이동 · scroll 줌 · WASD 중심점" 힌트를 다시 추가함.
2. **"피사체 중심으로 재정렬" 버튼이 안 눌리는 것처럼 느껴짐**: 실제로는 버튼 자체는 정상 동작했지만(`onClick` 연결 확인), v1.4.0에서 궤도 회전 방식으로 바꾸면서 이 버튼의 역할이 "피벗 오프셋만 (0,0,0)으로 리셋"으로 축소돼 있었다. 문제는 사용자가 WASD/스페이스+드래그로 피벗을 옮기지 *않은* 상태에서 버튼을 누르면 이미 오프셋이 0이라 시각적으로 아무 변화가 없어서 "안 눌린다"고 느꼈을 가능성이 크다. 샷 프리셋 스냅 로직(`applySnap`)을 재사용 가능한 함수로 뽑아내서, 재정렬 버튼이 "피벗 리셋"뿐 아니라 **현재 샷 타입 기준으로 각도/거리까지 통째로 다시 스냅**하도록 바꿨다 — 드래그로 궤도를 돌렸든 WASD로 피벗을 옮겼든, 버튼을 누르면 항상 눈에 띄게 원래 프레이밍으로 돌아온다.
3. **마네킹을 관절 계층 구조로 재설계**: 사용자가 참고 이미지(파란/주황 반반 컬러의 관절 인체 모형)를 주면서 "나중에 관절을 움직이는 기능을 넣고 싶다"고 요청. 기존 마네킹은 각 팔다리 부위가 전부 최상위 그룹에 절대좌표로 흩어져 있어서, 팔꿈치를 돌려도 손이 따라오지 않는 구조였다(리깅 불가능). `Mannequin.tsx`를 **관절마다 중첩된 그룹(hips → spine → shoulder → elbow → hand, hips → hip → knee → foot)**으로 전면 재작성해서, 각 관절 그룹의 `rotation`을 바꾸면 그 아래 자식 부위 전체가 함께 회전하는 정방향 기구학(forward kinematics) 구조로 만들었다. 각 관절 그룹은 `pose` prop(`leftShoulder`, `leftElbow`, `rightHip` 등, 라디안 `[x,y,z]`)을 받도록 미리 열어뒀고 기본값은 전부 `[0,0,0]`(기존과 동일한 중립 자세)이라 지금 당장은 외형·동작에 변화가 없다. 실제 "관절을 드래그해서 포즈를 바꾸는" UI는 아직 만들지 않았고, 이 prop에 값만 넣어주면 되는 구조까지만 준비한 상태 — 다음에 요청하면 포즈 조작 UI(예: 팔다리 각도 슬라이더, 또는 3D 기즈모)를 붙이면 된다.
   - 참고 이미지의 색/자세는 반영하지 않았고(사용자가 명시적으로 무시해달라고 함), 전체적인 비율만 좀 더 통통하게(가슴/팔다리 두께 증가, 관절 구를 더 크게, 손은 주먹 형태, 발은 살짝 기울어진 신발 형태) 조정했다. 머리/어깨/골반/무릎/발목의 월드 좌표 높이는 이전 버전과 동일하게 맞춰서 기존 샷 프리셋들(카메라 높이/거리 값)이 그대로 맞게 유지했다.
- **검증 한계**: 이번에도 실제 렌더링 화면은 못 보고, 관절 오프셋들이 기존 절대좌표와 정확히 일치하는지는 손계산으로 대조했다(문서에 계산 과정 남김). 실제로 비율이 어색하거나 관절 위치가 부자연스러우면 스크린샷으로 알려주는 게 필요하다.

## v1.7.0 — 마네킹 외형 추가 개선 + 스페이스바 커서 (2026-07-16)
1. **마네킹 외형을 참고 이미지에 더 가깝게**: 사용자가 다시 한번 참고 이미지(파란/주황 반반 컬러의 관절 인체 모형)를 주면서 "이런 외형으로 만들어달라는 의미였다"고 명확히 함. v1.6.0에서는 관절 계층 구조(리깅 가능한 구조)에 집중했었는데, 이번엔 실루엣 자체를 더 참고 이미지에 맞췄다.
   - 어깨: 작은 관절 구 대신 크고 둥근 "숄더 캡"(구체)이 팔과 자연스럽게 이어지도록 확대.
   - 허리: 가슴과 골반 사이에 토러스(도넛) 형태의 벨트 밴드 추가.
   - 어깨선: 양쪽 어깨를 가로지르는 칼라 밴드(가로로 눕힌 캡슐) 추가.
   - 팔꿈치/무릎: 살짝 납작하게(scale) 눌러서 "캡"처럼 보이게 하고 크기 확대. 무릎은 앞으로 살짝 튀어나오도록 z축 오프셋 추가.
   - 손: 손목에 얇은 밴드 + 그 아래 둥근 주먹.
   - 발: 뒤꿈치 박스 + 앞으로 갈수록 살짝 기울어진 발끝 박스, 2단 구성으로 신발 실루엣에 가깝게.
   - 골반/어깨/무릎/발 등 각 관절의 로컬 오프셋을 바꾸면서도, 머리 높이(1.66m)와 고관절 높이(0.82m)는 이전 버전과 정확히 같게 맞춰서 기존 샷 프리셋(카메라 거리/높이)이 그대로 맞도록 확인함(직접 손계산).
2. **스페이스바 = 손 모양 커서**: 스페이스바를 누르고 있을 때 캔버스 커서를 `grab`(잡기 전)/`grabbing`(드래그 중)으로 바꿔서, 지금 핸드 툴(팬) 모드인지 시각적으로 구분되게 함. 스페이스바를 떼면 커서를 `auto`로 복귀.
- **검증 한계**: 계속 반복되는 한계이지만, 이번에도 실제 렌더링 결과는 못 보고 좌표 계산으로만 정합성을 확인했다. 참고 이미지와 실제 얼마나 비슷해 보이는지는 사용자 확인이 반드시 필요.

## v1.8.0 — 구체관절 마네킹 + 카메라 피벗 버그 수정 (2026-07-16)
사용자가 같은 참고 이미지를 다시 첨부하며 "구체관절인형처럼 만들어달라, 고집부리지 마라"고 강하게 재요청. v1.7.0에서 "캡" 느낌을 내려고 팔꿈치/무릎/발목 관절 구를 `scale`로 납작하게 눌러놨었는데, 이게 오히려 구체관절인형 특유의 "관절이 볼록 튀어나온 구슬처럼 보이는" 느낌을 없애고 있었다는 걸 깨달음.
1. **관절을 전부 완전한 구체로 되돌림**: 팔꿈치/손목/무릎/발목에 걸려있던 `scale={[1,0.85,1]}` 같은 비균등 스케일을 전부 제거하고, 인접 팔다리 반지름보다 뚜렷하게 큰 완전한 구(sphere)로 키웠다(팔꿈치 0.072 vs 아래팔 0.05, 무릎 0.098 vs 종아리 0.078 등). 목 관절도 원기둥 대신 구체로 바꿔서 "모든 관절 = 구체"로 통일. 무릎에 줬던 forward z-offset(앞으로 튀어나오게 하던 것)도 제거해서 좌우 대칭이 깨지지 않게 함.
2. **중요 버그 발견: 카메라 피벗이 SUBJECT 슬라이더를 라이브로 쫓아가던 문제**: 사용자가 "인물1 슬라이더를 움직이면 피사체는 안 움직이고 배경(또는 인물2)이 움직인다"고 보고. 원인 확인: `CameraRig.tsx`의 `currentPivot()`이 `subject.leftRight/depth`를 매 프레임 라이브로 읽어서 피벗을 계속 갱신하고 있었다. 궤도 카메라가 항상 피벗을 정면으로 바라보므로, 슬라이더를 움직이면 카메라가 피사체를 실시간으로 쫓아가며 화면 중앙에 계속 고정시켜버렸다 — 결과적으로 피사체는 화면상 안 움직이는 것처럼 보이고, 카메라 자체가 월드 공간에서 이동하니 정지해 있는 방(배경)과 (카메라를 안 쫓아가는) 두 번째 피사체가 상대적으로 화면에서 미끄러지듯 움직이는 것처럼 보였던 것. SUBJECT 패널의 좌우/깊이 슬라이더는 원래 "고정된 프레임 안에서 피사체 위치를 구도상 옮기는" 용도였는데, 라이브 트래킹 때문에 사실상 무력화되어 있었다.
   - 수정: `pivotBaseRef`(피사체 기준 피벗 위치, 스냅 시점에만 고정)를 새로 추가하고, `currentPivot()`은 더 이상 `subject` 스토어를 실시간으로 읽지 않고 `pivotBaseRef + pivotOffsetRef`만 사용하도록 변경. `applySnap()`(샷 타입 선택/재정렬 시 호출)에서만 `pivotBaseRef`를 그 순간의 피사체 위치로 갱신한다.
   - Node로 검증: 스냅 후 subject.leftRight를 0.5로 바꿔도 카메라 위치가 전혀 안 바뀌는 것을 확인(이전 버그였다면 카메라가 쫓아가서 위치가 바뀌었을 것도 비교 출력으로 남김).
3. **화각비 크롭에 연한 가이드라인 추가**: v1.5.0에서 완전히 제거했던 크롭 경계선을, 이번엔 `border-white/15`(아주 연한 흰색, 1px)로 다시 추가했다. 이전의 진한 accent 색 보더와 달리 은은해서 다른 오버레이 요소와 시각적으로 덜 부딪히면서도 크롭 경계는 파악할 수 있게 함.
- **검증 한계**: 마네킹 형태는 이번에도 좌표/반지름 관계만 손으로 확인했고 실제 렌더링은 못 봤다. 카메라 피벗 버그는 명확한 로직 결함을 찾아 수정한 것이라 상대적으로 확신도가 높다.

## v1.9.0 — 마네킹 실루엣 재작업 + 버드아이 카메라 추적 버그 (2026-07-16)
사용자가 실제 배포 화면 스크린샷(v1.8.0)과 참고 이미지를 나란히 주면서 "로봇이 이 모양 그대로다, 색이 문제가 아니라 외형이 문제다"라고 명확히 지적. 색을 먼저 바꿨던 걸(관절 색 대비) 되돌리라는 게 아니라, **그 위에 실루엣 자체도 같이 고치라**는 뜻으로 이해하고 진행함.
1. **실제 배포 스크린샷 분석 결과**: 몸통이 위아래로 굵기가 똑같은 "알약(캡슐)" 한 덩어리라 뭉툭해 보였고, 허리 벨트가 굵은 토러스 도넛이라 튜브처럼 튀어나와 보였으며, 팔/다리가 위아래 굵기 차이가 거의 없어 테이퍼가 안 느껴졌고, 발이 작은 사각 블록이라 신발처럼 안 보였음. 참고 이미지는 가슴(넓음)→허리(좁음)의 뚜렷한 V자 테이퍼, 얇은 벨트 라인, 팔/다리의 확실한 위-아래 굵기 차이, 길쭉한 신발 모양 발이 특징.
2. **개선 내용**:
   - 몸통을 가슴 캡슐(반지름 0.18) + 허리 캡슐(반지름 0.13) 2단으로 분리해 V자 테이퍼 표현.
   - 허리 벨트를 토러스 튜브 반지름 0.03→0.013으로 대폭 축소해 "슬림한 띠"로.
   - 위팔(0.06)-아래팔(0.042), 허벅지(0.098)-종아리(0.062) 반지름 차이를 키워 테이퍼 강화.
   - 머리를 완전한 구 대신 `scale={[1, 1.18, 0.95]}`로 세로로 살짝 늘려 갸름한 타원형으로.
   - 발을 뒤꿈치 블록 + 길게 뻗은 발끝 블록(z축 길이 0.11→0.19로 확장) 2단 구성으로 재작업해 신발 실루엣에 가깝게.
   - 관절 y좌표(머리 1.66m, 고관절 0.82m 등)는 이전과 동일하게 유지해서 기존 샷 프리셋 카메라 값과 계속 맞도록 함.
3. **버드아이에서도 동일한 카메라 추적 버그가 남아있었음**: v1.8.0에서 "샷 뷰"의 피벗 라이브 트래킹 버그는 고쳤지만, **버드아이 전용 코드 경로에는 똑같은 패턴의 버그가 그대로 남아있었다** — `computeSubjectWorldPosition(subject.leftRight, subject.depth)`를 매 프레임 라이브로 읽어서 버드아이 중심을 갱신하고 있었음. `birdCenterBaseRef`를 추가해서 `applySnap()` 호출 시점에만 고정하도록 동일하게 수정. Node로 재검증: 스냅 후 슬라이더를 바꿔도 버드아이 중심 좌표가 그대로임을 확인.
- **검증 한계**: 실루엣 조정은 이번에도 실제 렌더링을 못 보고 반지름/오프셋 수치만 손으로 조정했다. 사용자가 준 실제 스크린샷 덕분에 어떤 부분이 문제인지는 비교적 정확히 짚었다고 생각하지만, 얼마나 개선됐는지는 다시 스크린샷으로 확인이 필요하다.

## v1.10.0 — 마네킹 근본 기술 한계 인지 및 LatheGeometry로 전면 교체 (2026-07-16)
사용자가 v1.9.0 배포 스크린샷을 다시 보내며 "왜 이 외형을 고집하냐, 레퍼런스를 줬는데 왜 안 바꾸냐"고 강하게 질문. 스크린샷을 다시 뜯어본 결과, **진짜 이유를 찾았다: 고집이 아니라 기술적 한계였다.**

- **근본 원인**: v1.6.0~v1.9.0 내내 몸통/팔/다리를 캡슐(capsule)과 구체(sphere)를 이어 붙이는 방식으로 만들어왔다. 캡슐은 길이 전체에서 반지름이 균일하고, 서로 다른 캡슐을 이어 붙이는 지점에는 항상 둥근 이음매(혹)가 생긴다. 그래서 반지름 숫자를 아무리 조정해도(v1.9.0에서 가슴/허리를 2단으로 나누는 식으로 시도) 참고 이미지처럼 매끈하게 이어지는 인체 실루엣이 나올 수 없었다 — 스택 방식 자체의 한계였지, 값을 안 바꿔서가 아니었다.
- **해결**: `THREE.LatheGeometry`(반지름-높이 프로파일을 Y축 기준으로 회전시켜 만드는 회전체 형상)로 몸통·위팔·아래팔·허벅지·종아리를 전부 다시 만들었다. 예) 몸통은 골반(반지름 0.15)→허리(0.125, 잘록)→가슴(0.175, 넓음)→목(0.07)까지 하나의 연속된 형상으로, 이음매 없이 매끈하게 테이퍼링된다. 관절(어깨/팔꿈치/손목/고관절/무릎/발목)은 여전히 별도의 구체로 남겨서 구체관절인형 특유의 "관절이 볼록한 구슬" 느낌은 그대로 유지.
- **팔다리 길이도 같이 늘림**: 기존 길이(허벅지 0.32+종아리 0.28=0.6m)가 성인 다리치고 짧아서 "다리가 짧고 통통해 보이는" 인상에도 일조하고 있었다. 허벅지 0.38+종아리 0.34=0.72m로 늘리고, 팔도 비슷한 비율로 늘림. 머리 높이(1.66m)·고관절 높이(0.82m) 등 기존 카메라 프리셋이 참조하는 주요 관절 높이는 그대로 유지해서 카메라 값 재조정이 필요 없게 했다.
- **반성**: 이전 몇 버전 동안 "숫자를 조금씩 조정"하는 식으로 대응했는데, 사용자가 계속 "그대로다"라고 느낀 게 당연했다 — 같은 기법(캡슐 스택) 안에서는 아무리 미세조정해도 근본적으로 다른 실루엣이 나올 수 없었다. 이번에 기법 자체를 바꾼 게 핵심이고, 앞으로 비슷한 상황(여러 번 수정해도 사용자가 "그대로다"라고 하는 경우)에서는 파라미터 조정이 아니라 기법/구조 자체에 근본적 한계가 있는 게 아닌지 먼저 의심해야 한다.
- **검증 한계**: 여전히 실제 렌더링은 못 보고 좌표·반지름 계산만 확인했다. LatheGeometry가 실제로 매끈하게 나오는지, 관절 구체와의 접합부가 자연스러운지는 사용자 확인이 필요하다.

## v1.11.0 — 이용 방법 페이지 + OG 이미지/메타 태그 (2026-07-16)
1. **이용 방법 페이지**: `/guide` 라우트로 정적 페이지 추가. 카메라 조작(궤도 회전/핸드 툴/줌/중심점 이동), 샷 타입, 카메라 패널, 피사체 패널, 프롬프트 패널, 결과 모달까지 섹션별로 정리. 헤더에 "이용 방법" 링크 추가.
2. **OG 이미지**: `src/app/opengraph-image.tsx`에서 `next/og`의 `ImageResponse`(satori 기반, 브라우저 없이 서버에서 JSX→PNG 렌더링)로 1200×630 이미지를 생성. 다크 배경 + "SHOTSTAGE" 워드마크(STAGE만 accent 주황) + 태그라인 + 뷰포트 코너 브라켓과 통일감 있는 모서리 장식.
   - **이번엔 실제로 렌더링 결과를 눈으로 확인했다**: 3D 캔버스와 달리 이 이미지는 빌드 시 정적 PNG로 생성되므로, `.next/server/app/opengraph-image.body`를 직접 `view` 도구로 열어서 실제로 확인함 (드디어 헤드리스 브라우저 없이도 결과물을 볼 수 있었던 케이스). PIL로 픽셀 샘플링해서 배경색(#06080d)과 주황/흰색 텍스트가 의도한 위치에 렌더링됐는지도 교차 검증.
3. **메타 태그**: Next.js App Router의 `opengraph-image.tsx` 파일 컨벤션을 쓰면 og:image/twitter:image 관련 태그가 자동으로 페이지 `<head>`에 삽입된다 — 따로 손으로 태그를 추가할 필요가 없었음. `layout.tsx`에 `metadataBase`(Vercel의 `VERCEL_URL` 자동 감지, 커스텀 도메인은 `NEXT_PUBLIC_SITE_URL` 환경변수로 덮어쓰기 가능)와 명시적 `openGraph`/`twitter` 메타데이터를 추가. `next build` 후 실제 프로덕션 서버(`next start`)를 띄워 `curl`로 og:*, twitter:* 태그가 정확히 주입되는지, `/guide`와 `/opengraph-image` 라우트가 200을 반환하는지 직접 확인함.

## v1.12.0 — Seedance 2.0 비디오 모델 추가 (2026-07-16)
사용자가 https://soylab.ai/seedance20 를 참고해 씨댄스(Seedance 2.0)를 비디오 모델 목록에 추가해달라고 요청.
- 웹 검색으로 Seedance 2.0(ByteDance의 멀티모달 오디오·비디오 동시 생성 모델) 프롬프트 가이드 확인. 핵심 원칙: (1) "누가 어떤 행동을 하는지"를 논리적 기반으로 먼저 명시, (2) 참조 자료는 "이미지 1의 구도를 사용하라" 식으로 자연어로 명확히 지정, (3) 오디오·비디오를 한 번에 생성하는 게 특징이라 장면에 맞는 앰비언트 사운드도 함께 명시하는 게 유리함.
- `VIDEO_MODELS`에 "Seedance 2.0" 추가 (Higgsfield, Runway, Kling과 함께).
- `promptBuilder.ts`에 `buildSeedancePrompt()` 전용 빌더 추가: 주체+행동+장소를 먼저 서술 → "첨부된 블로킹 프레임을 카메라 참고로 활용해 이런 움직임을 하라"는 자연어 참조 지정 → 캐릭터/환경시트가 있으면 "일관성 유지" 문구 → 스타일 → 마지막에 "장면에 맞는 자연스러운 앰비언트 사운드, 화각비" 문구.
- 이전까지 비디오 모드는 모델 상관없이 `buildGenericPrompt` 하나만 썼는데, Seedance 2.0만 전용 분기를 타도록 `buildFinalPrompt`의 비디오 분기도 이미지 모드와 동일하게 모델별 switch 구조로 바꿈 (Higgsfield/Runway/Kling은 계속 범용 템플릿 사용).

## v1.13.0 — Kling 전용 프롬프트 포맷 추가 (2026-07-16)
사용자가 "클링도 자료 찾아서 포맷 쓰게 하면 안 돼?"라고 요청. 웹 검색으로 Kling AI 프롬프트 가이드 여러 개 확인.
- 핵심 원칙: (1) Subject(구체적 묘사) + Action(정확한 움직임) + Context/Camera(3~5개 요소 이내) + Style(카메라·조명·분위기)의 4단 구조. (2) 카메라 움직임을 명시하지 않으면 정지 샷이 되어버림. (3) 움직임에 "끝맺음"이 없으면(open-ended motion) 생성이 자주 멈춘다(99% hang) — 반드시 "그러다가 다시 제자리로 돌아온다" 같은 종료 지점을 명시해야 함. (4) 같은 프롬프트 안에 상반된 카메라 지시("고정 카메라"+"패닝")를 동시에 넣지 말 것. (5) 형용사 나열("4K ultra HD cinematic masterpiece")은 도움이 안 되고 구체적 문장이 나음.
- `buildKlingPrompt()` 추가: 주체+맥락 문장 → "Camera: {무브}, {강도}, then holds a steady frame for the last moment." 식으로 항상 카메라를 명시하고 끝맺음을 붙임(무브가 static이어도 "정지 프레임을 계속 유지한다"고 명시) → 캐릭터/환경시트 있으면 매칭 문구 → 조명/분위기 문장 → 마지막에 지속시간·화각비.
- `VIDEO_MODELS`에서 "Kling"을 `buildFinalPrompt`의 비디오 분기 switch에 연결. Higgsfield/Runway는 계속 범용 템플릿 사용.

## v1.14.0 — OG 이미지를 정적 파일 참조 방식으로 전환 (2026-07-16)
사용자가 og.png를 직접 첨부하겠다며 경로만 만들어달라고 요청.
- `src/app/opengraph-image.tsx`(next/og 동적 생성 파일) 삭제. 정적 파일과 동적 생성 파일을 같이 두면 Next.js 파일 컨벤션이 우선시되어 충돌/중복 태그가 날 수 있어 하나만 남김.
- `layout.tsx`의 `openGraph.images`/`twitter.images`에 `/og.png`(1200×630)를 명시적으로 참조하도록 추가.
- 파일이 아직 없어도(`public/og.png` 미존재) `npm run build`가 에러 없이 정상 통과하는 것을 확인함 — 사용자가 나중에 파일만 넣으면 별도 코드 수정 없이 바로 적용됨.
- 경로 안내: `public/og.png`.

## v1.15.0 — 인물 2 전용 캐릭터시트/외형 지원 (2026-07-16)
사용자가 "피사체가 2개면 캐릭터시트도 2개여야 하지 않냐"고 지적. 확인해보니 캐릭터시트뿐 아니라 **인물 2의 외형을 설명하는 텍스트 입력 자체가 아예 없었다** — 3D 위치(좌우/깊이/회전)는 인물 2가 독립적인데, 프롬프트에는 "a second person"이라는 generic 텍스트로만 언급되고 있었음(나노바나나 빌더 한정, 다른 모델은 아예 언급도 없었음).
- `types/index.ts`의 `PromptFieldsState`에 `subject2`, `hasCharacterSheet2`, `characterSheetImage2` 추가.
- `PromptPanel.tsx`: `subject.showSecondSubject`가 켜져 있을 때만 "인물 2 캐릭터시트가 있어요" 체크박스+업로드, "인물 2 (피사체)" 텍스트 입력(프리셋 라이브러리 재사용)을 추가로 노출. 인물 1/2 필드 라벨 앞에 `SUBJECT_COLORS`와 동일한 색 점을 붙여서 3D 모델·SUBJECT 패널과 시각적 일관성 유지.
- `promptBuilder.ts`: `BuildPromptInput`에 `hasSecondSubject` 추가하고, 공용 헬퍼 `secondSubjectSentences()`를 만들어 모든 모델 빌더(Generic/Midjourney/GPT Image 2/Seedance/Kling)에 동일하게 삽입. 나노바나나 빌더는 기존 "a second person" 하드코딩을 `prompt.subject2`가 있으면 그 텍스트로 대체하고, `[Consistency Core]` 블록에도 인물 2 캐릭터시트 매칭 조건을 추가.
- `PromptPanel.tsx`의 `handleGenerate()`가 `hasSecondSubject: subject.showSecondSubject`를 `buildFinalPrompt`에 전달하도록 수정 (이게 누락되어 있어서 `npm run build`의 TypeScript 타입 체크에서 바로 잡혔음 — 타입 안전성 덕분에 배포 후가 아니라 빌드 단계에서 발견).

## v1.16.0 — 사용자 제공 og.png 반영 (2026-07-16)
사용자가 직접 만든 og.png(1731×909, "SHOTSTAGE 고도화 버전 작업 기록" 디자인)를 첨부하며 이후 버전부터 포함해달라고 요청.
- `public/og.png`로 저장. 프로덕션 서버(`next start`)를 띄워 실제로 `/og.png`가 200으로 서빙되고 `<meta property="og:image">` 태그가 정확히 참조하는지 curl로 확인.
- 실제 이미지 크기(1731×909, 약 1.9:1 — OG 권장 비율 1.91:1과 거의 동일)에 맞춰 `og:image:width`/`height` 메타데이터도 1200×630에서 1731×909로 수정.

## v1.16.1 — og:image 크기 원복 (2026-07-16)
사용자가 뷰포트의 그리드 토글 아이콘(▦)이 뭔지 물어봄 → 그리드 표시 켜기/끄기 버튼이라고 답변. 동시에 og.png를 1200×630으로 다시 만들어 주겠다며 메타데이터도 그 크기로 되돌려달라고 요청. `layout.tsx`의 `og:image:width/height`를 1731×909 → 1200×630으로 원복.

## v1.17.0 — 코너 브라켓 재배치 + 헤더 3분할 레이아웃 (2026-07-16)
1. **코너 브라켓이 라벨과 겹쳐서 잘려 보이던 문제**: 뷰포트 좌상단 장식 브라켓(`left-3 top-3`)이 바로 옆 샷 라벨 박스(`left-4 top-4`)와 위치가 거의 겹쳐서, 라벨 밑에 깔려 잘려 보였다. 처음엔 브라켓을 제거하려 했으나 사용자가 "없애지 말고 보이게 해달라"고 해서, 상단 대신 **겹치는 게 없는 하단 양쪽 모서리(좌하단·우하단)**로 재배치했다. 상단은 왼쪽(라벨), 오른쪽(그리드/Aa 버튼)이 이미 다 차 있어서 빈 자리가 없었음.
2. **헤더 단축키 안내 위치를 중앙으로**: 사용자가 스크린샷으로 헤더의 태그라인과 단축키 안내 사이 빈 공간을 가리키며 "여기로 옮기는 게 어떠냐"고 제안. 헤더를 `flex justify-between`(좌/우 2분할)에서 `grid grid-cols-[1fr_auto_1fr]`(좌/중앙/우 3분할)로 바꿔서, 단축키 안내를 중앙의 여유 공간에 배치했다. 덕분에 노출 breakpoint도 `lg`→`md`로 낮춰서 더 넓은 화면 범위에서 보이게 됨.

## v1.17.1 — 뷰포트 중복 힌트 텍스트 제거 (2026-07-16)
사용자가 스크린샷에 빨간 마크로 직접 표시: 헤더 중앙(이용 방법 왼쪽)의 단축키 안내는 그대로 유지, 뷰포트 안 "미디엄 샷" 라벨 옆에 중복으로 떠 있던 같은 내용의 힌트 텍스트는 삭제 요청. `ViewportOverlay.tsx`에서 해당 블록 제거, 샷 라벨만 남김.

## 확인되지 않은 가정 (오픈 이슈, PRD 9절과 동일)







































- 원본이 mm ↔ FOV(도) 환산 로직을 이미 갖고 있는지 미확인. UI에 두 단위가 혼용되어 있어(FOV 슬라이더가 도 단위, 라벨은 mm 단위) 있다고 가정하고 렌즈 프리셋을 설계함.
- 캐릭터/환경 참조 이미지를 IndexedDB에 저장할 때 브라우저별 용량 제한을 고려해 리사이즈/압축이 필요할 수 있음. 아직 구체적인 리사이즈 기준(최대 해상도 등)은 정하지 않음 — 구현 시점에 결정 필요.
